Run the Vitest test suite for businessLogic.ts.

```bash
cd "/Users/apple/phucsang app/QU-N-TR-C-A-H-NG" && npm test
```

Expected: 43 tests pass. Coverage: calculateSeniority, cleanVNNumber, parseVNDate, determineCurrentPolicy, isUUID, generateId, isStaffActive, normalizeHeader.

If any test fails, read the error output carefully and check businessLogic.ts for the broken function. Do NOT change tests to match wrong behavior — fix the source code instead.
